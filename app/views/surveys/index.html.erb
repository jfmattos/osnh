
<div class="container">
    <ul class="m-0 p-0">
        <% @survey.each do |survey| %>
          <div>
            <li>
              <%= link_to survey.title, survey_path(survey), id:"survey-btn", class: "btn btn-primary d-flex align-items-center text-center" %>
            </li>
          </div>
        <% end %>
    </ul>
</div>
