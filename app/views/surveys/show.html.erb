<div class="container">
  <% content_for :meta_title, "#{@survey.title} is on #{DEFAULT_META["meta_product_name"]}" %>

  <h1 class="text-center my-4"><%= @survey.title %></h1>
</div>
<div class="container d-flex flex-column">
  <span class="btn btn-primary btn-lg w-100 mt-5">
    <%= link_to "Start survey",
        new_survey_user_answer_path(@survey, question_index: 0),
        class: "text-light text-decoration-none strong" %>
  </span>
</div>
